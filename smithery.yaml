# Smithery: https://smithery.ai/docs
name: "@attestd/mcp"
description: "CVE vulnerability, supply chain integrity, product catalog, and CVE detail tools for Claude Code and MCP clients (Attestd API)"
startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      ATTESTD_API_KEY:
        type: string
        description: "Attestd API key (atst_...) — required for check_package_vulnerability, check_batch_vulnerabilities, get_cve_details, and live list_covered_products"
      ATTESTD_BASE_URL:
        type: string
        description: "Optional API base URL override (default https://api.attestd.io)"
    required: []
  commandFunction: |-
    (config) => ({
      command: "npx",
      args: ["-y", "@attestd/mcp"],
      env: {
        ATTESTD_API_KEY: config.ATTESTD_API_KEY || "",
        ATTESTD_BASE_URL: config.ATTESTD_BASE_URL || ""
      }
    })
